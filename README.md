# TabView

基于 Electron 22 + Vue 3 的多标签页浏览器框架(支持win7)，支持嵌入前端项目或固定网址，同域 localStorage 共享，自定义下载管理。

> Electron 23 升级到了 Chromium 110。Chromium 官方从 109 版本之后停止了对 Windows 7/8/8.1 的支持。
因此，Electron 23 及之后的版本均无法在 Windows 7 上运行。

## 特性

- 🖥️ **多标签页** - 使用 BrowserView 实现真正的浏览器标签页效果
- 🔗 **同域存储共享** - 所有标签页共享同一个 session，localStorage 自动同步
- 📥 **下载劫持** - 自定义下载管理器，支持进度显示、文件操作
- 🎨 **Vue 3 界面** - 现代化的 Vue 3 + 组合式 API 构建的 UI
- 🪟 **独立下载面板** - 子窗口实现，避免被 BrowserView 遮挡
- 🏢 **嵌入支持** - 支持本地开发服务器或固定网址
- 🪟 **Win7 兼容** - 基于 Electron 22，支持 Windows 7

## 技术栈

| 技术         | 版本   | 说明            |
|------------|------|---------------|
| Electron   | 22.x | 支持 Win7 的最高版本 |
| Vue        | 3.x  | 组合式 API       |
| TypeScript | 4.x  | 全类型支持         |
| Vite       | 5.x  | 构建工具          |

## 安装

```bash
# 克隆项目
git clone https://github.com/korylee/tab-view.git
cd tabview

# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build
```

## 配置

### 初始标签页

在 electron/main.ts 中修改：

```ts
const urls = [
  'http://localhost:3000'  // 你的本地项目
];

urls.forEach((url, i) => {
  const id = tabManager.createTab(url);
  if (i === 0) tabManager.switchTab(id);
});
```

## Electron22兼容性说明

### 已知限制

| 特性                           | Electron 22 | 解决方案                  |
|------------------------------|-------------|-----------------------|
| `getCurrentBytesPerSecond()` | ❌ 不存在       | 手动计算速度                |
| `updated` 事件触发               | ⚠️ 不稳定      | 事件 + 轮询双保险            |
| `setSavePath` 时机             | ⚠️ 必须同步     | 先设临时路径，再异步确认          |
| `savePath` 属性                | ❌ 只读        | 使用 `setSavePath()` 方法 |

## 下载流程

```plain

will-download 事件
    ↓
e.preventDefault() 阻止默认下载
    ↓
item.setSavePath(tempPath) 设置临时路径
    ↓
dialog.showSaveDialog() 显示保存对话框
    ↓
用户确认后，监听 updated/done 事件
    ↓
下载完成后 移动到目标位置
```


## 开发计划
- [x] 标签页拖拽排序
- [ ] 会话恢复（重启后恢复标签页）
- [ ] 隐私模式（临时 session）
- [ ] 多窗口同步
- [ ] 历史记录
